# Менеджер профилей для сервиса знакомств (FastAPI проект)

## Описание проекта

Проект представляет собой REST API для управления профилями пользователей.  
Реализованы функции создания, получения, обновления и удаления профилей.  
API построено с использованием **FastAPI**, **SQLAlchemy (Async)** и **SQLite**.

---

## Основные возможности

- Веб-интерфейс для удобной работы с профилями
- Добавление нового профиля через HTML-форму
- Просмотр списка всех профилей в виде карточек
- Редактирование данных существующего профиля
- Удаление профиля с подтверждением
- Валидация данных (возраст от 18 до 100 лет)
- Адаптивный дизайн, работающий на разных устройствах

---

## Технологии

- **Python 3.11+**
- **FastAPI**
- **SQLAlchemy (async)**
- **Pydantic**
- **SQLite (через aiosqlite)**
- **Jinja2**
- **HTML5/CSS3**
- **Uvicorn** — сервер для запуска приложения

---

## Структура проекта

```
dating_profile_manager/
├── app/
│   ├── __init__.py
│   ├── main.py                    # Точка входа в приложение
│   ├── database.py                # Настройка подключения к БД
│   ├── dependencies.py            # Зависимости и сессия БД
│   ├── models.py                  # SQLAlchemy модели
│   ├── schemas.py                 # Pydantic-схемы (валидация)
│   │
│   ├── crud/                      # Логика работы с данными
│   │   ├── __init__.py
│   │   └── profiles.py
│   │
│   ├── routers/                   # Маршруты FastAPI
│   │   ├── __init__.py
│   │   ├── profiles.py           # REST API маршруты
│   │   └── pages.py              # Маршруты для веб-страниц
│   │
│   ├── static/                    # Статические файлы
│   │   ├── css/
│   │   │   └── style.css         # Стили приложения
│   │   └── dating-profile-manager-icon.png
│   │
│   └── templates/                 # HTML шаблоны
│       ├── base.html             # Базовый шаблон
│       ├── index.html            # Главная страница
│       ├── create.html           # Страница создания профиля
│       └── edit.html             # Страница редактирования
│
├── database.db                    # Файл базы данных SQLite
├── requirements.txt               # Список зависимостей проекта
└── README.md                      # Документация проекта
```

---

## Установка и запуск

### Клонировать репозиторий

```
git clone https://github.com/rrslamerr/dating-profile-manager
cd dating-profile-manager
```

### Создать виртуальное окружение

```
python -m venv venv
venv\Scripts\activate        # Для Windows
```

### Установить зависимости

```
pip install -r requirements.txt
```

### Запустить приложение

```
uvicorn app.main:app --reload
```

### Перейти по адресу

```
# Веб-интерфейс:
http://127.0.0.1:8000/

# Документация API:
http://127.0.0.1:8000/docs
```

---

## Пример работы API

```
{
  "name": "Иван",
  "age": 22,
  "description": "Студент ИБ",
  "interests": "Кибербезопасность"
}
```

Ответ:

```
{
  "id": 1,
  "name": "Иван",
  "age": 22,
  "description": "Студент ИБ",
  "interests": "Кибербезопасность"
}
```